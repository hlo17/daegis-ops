name: md-typography-guard

on:
  pull_request:
    paths: ["**/*.md"]

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: simple check
        run: |
          echo "OK: basic markdown guard placeholder"
